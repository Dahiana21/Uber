import React from "react";
import Layout from "../components/Layout";
import LoginFormSmart from '../containers/LoginFormSmart'

export default function SignInScreen() {
    return (
        <Layout>
            <LoginFormSmart />
        </Layout>
    );
}